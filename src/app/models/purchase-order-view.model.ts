export interface PurchaseOrderView {
  poNumber: string;
  poDate: string;
  organisationName: string;
  buyerDesg: string;
  buyerContact: string;

  productName: string;
  brand: string;
  model: string;
  unit: string;
  quantity: string;
  unitPrice: string;
  totalPrice: string;
}
